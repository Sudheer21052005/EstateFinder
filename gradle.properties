# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# AndroidX package structure (required since AndroidX is used everywhere).
android.useAndroidX=true
# Only keep the module's own resources in its R class (smaller, faster builds).
android.nonTransitiveRClass=true
